spring.application.name=watermanagementbackend

#spring.datasource.url=jdbc:postgresql://localhost:5432/watermanagement
#spring.datasource.username=postgres
#spring.datasource.password=1234567890
#spring.jpa.hibernate.ddl-auto=update
#
#frontend.allowed-origins=http://localhost:3000,http://127.0.0.1:5500


server.port=${PORT:8080}
spring.datasource.url=${DATASOURCE_URL}
spring.datasource.username=${DATASOURCE_USER}
spring.datasource.password=${DATASOUCE_PASSWORD}
spring.jpa.hibernate.ddl-auto=update

frontend.allowed-origins=${ALLOWEDORIGINS}